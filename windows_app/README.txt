HAKAI PROTOCOL — Windows Desktop App
======================================

Double-click "Hakai Protocol.exe" to launch.

The app opens in a clean app window (no browser toolbar).
Internet connection required (loads from hakai-protocol-v2.github.io).

If it doesn't open, make sure Microsoft Edge or Google Chrome is installed.

Requires: Windows 10 or 11
